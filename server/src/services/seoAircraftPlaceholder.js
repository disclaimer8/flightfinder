'use strict';

function aircraftPlaceholder() {
  return `<section class="aircraft-placeholder">
<h3>Aircraft assignments</h3>
<p>We are currently expanding per-airline aircraft data for every route. Until that ships, you can browse by aircraft type at <a href="/by-aircraft">/by-aircraft</a>.</p>
</section>`;
}

module.exports = { aircraftPlaceholder };
